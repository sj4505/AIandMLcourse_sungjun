# Week 3 Neural Network PySide6 앱 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Week 3의 5개 신경망 실험(Perceptron, Activation Functions, Forward Propagation, MLP, Universal Approximation)을 PySide6 GUI 앱으로 구현하고, PRD/TRD를 PDF로 제출한다.

**Architecture:** `week3/week3_app.py` 단일 파일에 MainWindow + 5개 LabTab + TrainingWorker(QThread)를 구성한다. 기존 01~05 스크립트는 수정 없이 클래스/함수만 import한다. 그래프는 FigureCanvasQTAgg로 앱 내부에 표시한다.

**Tech Stack:** PySide6, NumPy, Matplotlib (FigureCanvasQTAgg), reportlab (PDF)

---

## 파일 구조

| 파일 | 역할 |
|------|------|
| `week3/week3_app.py` | 신규 — 전체 PySide6 앱 (MainWindow, 5개 LabTab, TrainingWorker) |
| `week3/prd.md` | 신규 — PRD 마크다운 원본 |
| `week3/trd.md` | 신규 — TRD 마크다운 원본 |
| `week3/outputs/prd.pdf` | 신규 — PRD PDF 산출물 |
| `week3/outputs/trd.pdf` | 신규 — TRD PDF 산출물 |
| `pyproject.toml` | 수정 — PySide6 의존성 추가 |
| `week3/01_perceptron.py` | 유지 — Perceptron 클래스 import용 |
| `week3/02_activation_functions.py` | 유지 — 활성화 함수 import용 |
| `week3/03_forward_propagation.py` | 유지 — SimpleNetwork 클래스 import용 |
| `week3/04_mlp_numpy.py` | 유지 — MLP 클래스 import용 |
| `week3/05_universal_approximation.py` | 유지 — UniversalApproximator 클래스 import용 |

---

## Task 1: PySide6 의존성 추가

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: pyproject.toml에 PySide6 추가**

```toml
[project]
name = "aicoursework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tensorflow>=2.15.0",
    "numpy>=1.26.0",
    "matplotlib>=3.8.0",
    "reportlab",
    "seaborn>=0.13.2",
    "scipy>=1.16.3",
    "PySide6>=6.6.0",
]
```

- [ ] **Step 2: 의존성 설치 확인**

```bash
cd "C:\Users\ansun\OneDrive\바탕 화면\AIandMLcourse"
uv sync
```

Expected: `PySide6` 설치 완료 메시지

- [ ] **Step 3: PySide6 import 확인**

```bash
uv run python -c "from PySide6.QtWidgets import QApplication; print('PySide6 OK')"
```

Expected: `PySide6 OK`

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml
git commit -m "feat: add PySide6 dependency"
```

---

## Task 2: week3_app.py — 뼈대 (MainWindow + 탭 구조)

**Files:**
- Create: `week3/week3_app.py`

- [ ] **Step 1: MainWindow 뼈대 작성**

`week3/week3_app.py` 전체 내용:

```python
"""Week 3 Neural Network 실험 앱 — PySide6"""
import sys
import numpy as np
import matplotlib
matplotlib.use("QtAgg")
import matplotlib.pyplot as plt
from matplotlib.backends.backend_qtagg import FigureCanvasQTAgg as FigureCanvas
from matplotlib.figure import Figure

from PySide6.QtWidgets import (
    QApplication, QMainWindow, QTabWidget, QWidget, QVBoxLayout,
    QHBoxLayout, QSplitter, QFormLayout, QPushButton, QProgressBar,
    QTextEdit, QLabel, QSpinBox, QDoubleSpinBox, QComboBox, QCheckBox,
    QMessageBox,
)
from PySide6.QtCore import Qt, QThread, Signal
from PySide6.QtGui import QFont


class TrainingWorker(QThread):
    """백그라운드 연산 스레드"""
    progress = Signal(int)
    log_message = Signal(str)
    finished = Signal(object)   # list[Figure]
    error = Signal(str)

    def __init__(self, task_fn, params: dict):
        super().__init__()
        self._task_fn = task_fn
        self._params = params

    def run(self):
        try:
            figures = self._task_fn(self._params, self.progress, self.log_message)
            self.finished.emit(figures)
        except Exception as e:
            self.error.emit(str(e))


class BaseLabTab(QWidget):
    """모든 LabTab의 공통 베이스"""

    run_started = Signal()
    run_finished = Signal()

    def __init__(self, parent=None):
        super().__init__(parent)
        self._worker = None
        self._canvas_list: list[FigureCanvas] = []
        self._setup_ui()

    # ── 서브클래스가 구현할 메서드 ──────────────────────────────
    def _build_param_panel(self, form: QFormLayout):
        """파라미터 위젯을 form에 추가. 서브클래스 구현."""
        raise NotImplementedError

    def _collect_params(self) -> dict:
        """현재 위젯 값을 dict로 반환. 서브클래스 구현."""
        raise NotImplementedError

    def _task_fn(self, params: dict, progress_cb, log_cb) -> list:
        """연산 수행 후 Figure 리스트 반환. 서브클래스 구현."""
        raise NotImplementedError

    # ── 공통 UI 구성 ────────────────────────────────────────────
    def _setup_ui(self):
        splitter = QSplitter(Qt.Horizontal, self)

        # 왼쪽: 파라미터 패널
        left = QWidget()
        left.setMaximumWidth(260)
        left_layout = QVBoxLayout(left)
        form = QFormLayout()
        self._build_param_panel(form)
        left_layout.addLayout(form)
        self._run_btn = QPushButton("실행")
        self._run_btn.setFont(QFont("", 12, QFont.Bold))
        self._run_btn.clicked.connect(self._on_run)
        left_layout.addWidget(self._run_btn)
        left_layout.addStretch()

        # 오른쪽: 결과 패널
        right = QWidget()
        right_layout = QVBoxLayout(right)
        self._progress = QProgressBar()
        self._progress.setRange(0, 100)
        right_layout.addWidget(self._progress)
        self._log = QTextEdit()
        self._log.setReadOnly(True)
        self._log.setMaximumHeight(120)
        right_layout.addWidget(self._log)
        self._canvas_area = QVBoxLayout()
        right_layout.addLayout(self._canvas_area)

        splitter.addWidget(left)
        splitter.addWidget(right)
        splitter.setStretchFactor(1, 1)

        main = QVBoxLayout(self)
        main.addWidget(splitter)

    def _on_run(self):
        try:
            params = self._collect_params()
        except ValueError as e:
            QMessageBox.warning(self, "파라미터 오류", str(e))
            return

        self._log.clear()
        self._progress.setValue(0)
        self._clear_canvases()

        self.run_started.emit()
        self._run_btn.setEnabled(False)

        self._worker = TrainingWorker(self._task_fn, params)
        self._worker.progress.connect(self._progress.setValue)
        self._worker.log_message.connect(self._append_log)
        self._worker.finished.connect(self._on_finished)
        self._worker.error.connect(self._on_error)
        self._worker.start()

    def _on_finished(self, figures):
        for fig in figures:
            canvas = FigureCanvas(fig)
            self._canvas_area.addWidget(canvas)
            self._canvas_list.append(canvas)
        self._progress.setValue(100)
        self._run_btn.setEnabled(True)
        self.run_finished.emit()

    def _on_error(self, msg):
        QMessageBox.critical(self, "오류", msg)
        self._run_btn.setEnabled(True)
        self.run_finished.emit()

    def _append_log(self, msg):
        self._log.append(msg)

    def _clear_canvases(self):
        for canvas in self._canvas_list:
            self._canvas_area.removeWidget(canvas)
            canvas.deleteLater()
        self._canvas_list.clear()

    def set_run_enabled(self, enabled: bool):
        self._run_btn.setEnabled(enabled)


class MainWindow(QMainWindow):
    def __init__(self):
        super().__init__()
        self.setWindowTitle("Week 3: Neural Network 실험")
        self.resize(1100, 750)

        self._tabs = QTabWidget()
        self.setCentralWidget(self._tabs)

        # ── Lab 탭 등록 (Task 3~7에서 구현) ─────────────────────
        from week3_app import Lab1Tab, Lab2Tab, Lab3Tab, Lab4Tab, Lab5Tab
        self._lab_tabs: list[BaseLabTab] = [
            Lab1Tab(), Lab2Tab(), Lab3Tab(), Lab4Tab(), Lab5Tab()
        ]
        labels = [
            "Lab1: Perceptron",
            "Lab2: Activation Fn",
            "Lab3: Forward Prop",
            "Lab4: MLP/XOR",
            "Lab5: Universal Approx",
        ]
        for tab, label in zip(self._lab_tabs, labels):
            self._tabs.addTab(tab, label)
            tab.run_started.connect(self._disable_all_runs)
            tab.run_finished.connect(self._enable_all_runs)

    def _disable_all_runs(self):
        for tab in self._lab_tabs:
            tab.set_run_enabled(False)

    def _enable_all_runs(self):
        for tab in self._lab_tabs:
            tab.set_run_enabled(True)


if __name__ == "__main__":
    app = QApplication(sys.argv)
    win = MainWindow()
    win.show()
    sys.exit(app.exec())
```

> **참고:** Task 3~7에서 각 Lab 클래스를 같은 파일에 추가한다. 위의 `from week3_app import ...` 구문은 Task 7 완료 후 제거하고 직접 참조로 바꾼다.

- [ ] **Step 2: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add week3 app skeleton with MainWindow and BaseLabTab"
```

---

## Task 3: Lab1Tab — Perceptron

**Files:**
- Modify: `week3/week3_app.py` (Lab1Tab 클래스 추가)

- [ ] **Step 1: Lab1Tab 클래스를 week3_app.py에 추가**

`MainWindow` 클래스 정의 **위**에 삽입:

```python
import sys
import os
sys.path.insert(0, os.path.dirname(__file__))

# week3 기존 코드에서 Perceptron 클래스 import
from importlib import import_module as _imp

def _import_perceptron():
    import importlib.util, types
    spec = importlib.util.spec_from_file_location(
        "p01", os.path.join(os.path.dirname(__file__), "01_perceptron.py")
    )
    mod = importlib.util.module_from_spec(spec)
    # plt.show / savefig 억제
    import matplotlib.pyplot as _plt
    _orig_show = _plt.show
    _orig_save = _plt.Figure.savefig
    _plt.show = lambda *a, **k: None
    spec.loader.exec_module(mod)
    _plt.show = _orig_show
    return mod

_p01 = _import_perceptron()
Perceptron = _p01.Perceptron


class Lab1Tab(BaseLabTab):
    """Lab 1: Perceptron — AND/OR/XOR 게이트"""

    def _build_param_panel(self, form: QFormLayout):
        self._gate = QComboBox()
        self._gate.addItems(["AND", "OR", "XOR"])
        form.addRow("Gate:", self._gate)

        self._lr = QDoubleSpinBox()
        self._lr.setRange(0.01, 1.0)
        self._lr.setSingleStep(0.01)
        self._lr.setValue(0.1)
        form.addRow("Learning Rate:", self._lr)

        self._epochs = QSpinBox()
        self._epochs.setRange(10, 10000)
        self._epochs.setValue(100)
        form.addRow("Epochs:", self._epochs)

    def _collect_params(self) -> dict:
        return {
            "gate": self._gate.currentText(),
            "lr": self._lr.value(),
            "epochs": self._epochs.value(),
        }

    def _task_fn(self, params, progress_cb, log_cb):
        import numpy as np
        import matplotlib
        matplotlib.use("QtAgg")
        import matplotlib.pyplot as plt

        X = np.array([[0,0],[0,1],[1,0],[1,1]], dtype=float)
        gate_map = {
            "AND": np.array([0,0,0,1]),
            "OR":  np.array([0,1,1,1]),
            "XOR": np.array([0,1,1,0]),
        }
        y = gate_map[params["gate"]]

        p = Perceptron(input_size=2, learning_rate=params["lr"])
        epochs = params["epochs"]
        for i, epoch in enumerate(range(epochs)):
            for inputs, label in zip(X, y):
                pred = p.predict(inputs)
                err = label - pred
                p.weights += params["lr"] * err * inputs
                p.bias += params["lr"] * err
            progress_cb(int((i+1)/epochs*100))

        # 결과 로그
        correct = sum(p.predict(x) == l for x, l in zip(X, y))
        log_cb(f"Gate: {params['gate']}  정확도: {correct}/4")

        # 그래프
        fig, ax = plt.subplots(figsize=(5, 4))
        x_min, x_max, y_min, y_max = -0.5, 1.5, -0.5, 1.5
        xx, yy = np.meshgrid(
            np.linspace(x_min, x_max, 100),
            np.linspace(y_min, y_max, 100),
        )
        Z = np.array([p.predict(np.array([xi, yi]))
                      for xi, yi in zip(xx.ravel(), yy.ravel())])
        Z = Z.reshape(xx.shape)
        ax.contourf(xx, yy, Z, alpha=0.3, levels=[-0.5,0.5,1.5], colors=["blue","red"])
        for point, label in zip(X, y):
            c = "red" if label == 1 else "blue"
            m = "o" if label == 1 else "x"
            ax.scatter(point[0], point[1], c=c, marker=m, s=200,
                       edgecolors="black", linewidth=2)
        ax.set_title(f"{params['gate']} Gate — Decision Boundary")
        ax.set_xlabel("x1"); ax.set_ylabel("x2")
        ax.grid(True, alpha=0.3)
        fig.tight_layout()
        return [fig]
```

- [ ] **Step 2: 앱 실행 확인 (Lab1만)**

```bash
cd "C:\Users\ansun\OneDrive\바탕 화면\AIandMLcourse\week3"
uv run week3_app.py
```

Expected: 앱이 열리고 Lab1 탭에서 실행 버튼 클릭 시 그래프 표시

- [ ] **Step 3: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add Lab1Tab Perceptron"
```

---

## Task 4: Lab2Tab — Activation Functions

**Files:**
- Modify: `week3/week3_app.py`

- [ ] **Step 1: Lab2Tab 클래스 추가** (`Lab1Tab` 클래스 아래에 삽입)

```python
class Lab2Tab(BaseLabTab):
    """Lab 2: Activation Functions 비교"""

    def _build_param_panel(self, form: QFormLayout):
        self._x_min = QDoubleSpinBox()
        self._x_min.setRange(-20.0, 0.0)
        self._x_min.setValue(-5.0)
        form.addRow("X 최솟값:", self._x_min)

        self._x_max = QDoubleSpinBox()
        self._x_max.setRange(0.0, 20.0)
        self._x_max.setValue(5.0)
        form.addRow("X 최댓값:", self._x_max)

        self._cb_sigmoid = QCheckBox("Sigmoid"); self._cb_sigmoid.setChecked(True)
        self._cb_tanh    = QCheckBox("Tanh");    self._cb_tanh.setChecked(True)
        self._cb_relu    = QCheckBox("ReLU");    self._cb_relu.setChecked(True)
        self._cb_leaky   = QCheckBox("Leaky ReLU"); self._cb_leaky.setChecked(True)
        for cb in [self._cb_sigmoid, self._cb_tanh, self._cb_relu, self._cb_leaky]:
            form.addRow("", cb)

    def _collect_params(self) -> dict:
        selected = []
        if self._cb_sigmoid.isChecked(): selected.append("Sigmoid")
        if self._cb_tanh.isChecked():    selected.append("Tanh")
        if self._cb_relu.isChecked():    selected.append("ReLU")
        if self._cb_leaky.isChecked():   selected.append("Leaky ReLU")
        if not selected:
            raise ValueError("최소 하나의 함수를 선택하세요.")
        return {
            "x_min": self._x_min.value(),
            "x_max": self._x_max.value(),
            "selected": selected,
        }

    def _task_fn(self, params, progress_cb, log_cb):
        import numpy as np
        import matplotlib
        matplotlib.use("QtAgg")
        import matplotlib.pyplot as plt

        x = np.linspace(params["x_min"], params["x_max"], 300)
        sel = params["selected"]

        def sigmoid(x): return 1/(1+np.exp(-np.clip(x,-500,500)))
        def tanh(x):    return np.tanh(x)
        def relu(x):    return np.maximum(0, x)
        def leaky(x):   return np.where(x>0, x, 0.01*x)

        fn_map = {
            "Sigmoid":    (sigmoid,  lambda x: sigmoid(x)*(1-sigmoid(x))),
            "Tanh":       (tanh,     lambda x: 1-np.tanh(x)**2),
            "ReLU":       (relu,     lambda x: np.where(x>0,1,0).astype(float)),
            "Leaky ReLU": (leaky,    lambda x: np.where(x>0,1,0.01)),
        }

        fig, axes = plt.subplots(1, 2, figsize=(10, 4))
        progress_cb(30)

        for name in sel:
            fn, dfn = fn_map[name]
            axes[0].plot(x, fn(x), label=name, linewidth=2)
            axes[1].plot(x, dfn(x), label=f"{name}'", linewidth=2)

        for ax, title, ylabel in zip(
            axes,
            ["Activation Functions", "Derivatives (Gradients)"],
            ["f(x)", "f'(x)"],
        ):
            ax.axhline(0, color="k", alpha=0.3)
            ax.axvline(0, color="k", alpha=0.3)
            ax.set_title(title, fontweight="bold")
            ax.set_xlabel("x"); ax.set_ylabel(ylabel)
            ax.legend(); ax.grid(True, alpha=0.3)

        progress_cb(100)
        log_cb(f"표시 함수: {', '.join(sel)}")
        fig.tight_layout()
        return [fig]
```

- [ ] **Step 2: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add Lab2Tab Activation Functions"
```

---

## Task 5: Lab3Tab — Forward Propagation

**Files:**
- Modify: `week3/week3_app.py`

- [ ] **Step 1: Lab3Tab 클래스 추가**

```python
class Lab3Tab(BaseLabTab):
    """Lab 3: Forward Propagation 단계별 시각화"""

    def _build_param_panel(self, form: QFormLayout):
        self._hidden = QSpinBox()
        self._hidden.setRange(2, 20)
        self._hidden.setValue(3)
        form.addRow("Hidden Units:", self._hidden)

        self._x1 = QDoubleSpinBox()
        self._x1.setRange(-2.0, 2.0); self._x1.setSingleStep(0.1); self._x1.setValue(0.5)
        form.addRow("Input X1:", self._x1)

        self._x2 = QDoubleSpinBox()
        self._x2.setRange(-2.0, 2.0); self._x2.setSingleStep(0.1); self._x2.setValue(0.8)
        form.addRow("Input X2:", self._x2)

    def _collect_params(self) -> dict:
        return {
            "hidden": self._hidden.value(),
            "x1": self._x1.value(),
            "x2": self._x2.value(),
        }

    def _task_fn(self, params, progress_cb, log_cb):
        import numpy as np
        import matplotlib
        matplotlib.use("QtAgg")
        import matplotlib.pyplot as plt
        import matplotlib.patches as mpatches

        n_h = params["hidden"]
        X = np.array([params["x1"], params["x2"]])

        np.random.seed(42)
        W1 = np.random.randn(2, n_h) * 0.5
        b1 = np.random.randn(n_h) * 0.1
        W2 = np.random.randn(n_h, 1) * 0.5
        b2 = np.random.randn(1) * 0.1

        def sigmoid(x): return 1/(1+np.exp(-x))
        def relu(x):    return np.maximum(0, x)

        z1 = X @ W1 + b1
        a1 = relu(z1)
        z2 = a1 @ W2 + b2
        a2 = sigmoid(z2)

        progress_cb(50)
        log_cb(f"z1 = {np.round(z1,3)}")
        log_cb(f"a1 (ReLU) = {np.round(a1,3)}")
        log_cb(f"z2 = {z2[0]:.4f},  a2 (Sigmoid) = {a2[0]:.4f}")

        fig, axes = plt.subplots(1, 2, figsize=(10, 4))

        # 왼쪽: 네트워크 다이어그램
        ax = axes[0]
        ax.set_xlim(0, 4); ax.set_ylim(0, 4); ax.axis("off")
        ax.set_title(f"Network Architecture (2-{n_h}-1)", fontweight="bold")
        iy = np.linspace(0.5, 3.5, 2)
        hy = np.linspace(0.5, 3.5, n_h)
        oy = [2.0]
        for y in iy:
            ax.add_patch(mpatches.Circle((0.5,y),0.2,color="lightblue",ec="black",lw=2))
        for y in hy:
            ax.add_patch(mpatches.Circle((2,y),0.2,color="lightgreen",ec="black",lw=2))
        ax.add_patch(mpatches.Circle((3.5,2),0.2,color="lightcoral",ec="black",lw=2))
        for iy_ in iy:
            for hy_ in hy:
                ax.plot([0.7,1.8],[iy_,hy_],"k-",alpha=0.3,lw=1)
        for hy_ in hy:
            ax.plot([2.2,3.3],[hy_,2],"k-",alpha=0.3,lw=1)
        ax.text(0.5,-0.1,"Input",ha="center",fontsize=9,fontweight="bold")
        ax.text(2,-0.1,f"Hidden\n(ReLU)",ha="center",fontsize=9,fontweight="bold")
        ax.text(3.5,-0.1,"Output\n(Sigmoid)",ha="center",fontsize=9,fontweight="bold")

        # 오른쪽: z/a 값 비교 (처음 3개만 표시)
        ax = axes[1]
        show = min(n_h, 6)
        idx = np.arange(show)
        w = 0.3
        ax.bar(idx - w/2, z1[:show], w, label="z1 (before ReLU)", color="orange", alpha=0.8)
        ax.bar(idx + w/2, a1[:show], w, label="a1 (after ReLU)",  color="green",  alpha=0.8)
        ax.set_title("Hidden Layer: z1 vs a1 (ReLU)", fontweight="bold")
        ax.set_xlabel("Neuron"); ax.set_ylabel("Value")
        ax.set_xticks(idx); ax.set_xticklabels([f"H{i+1}" for i in range(show)])
        ax.legend(); ax.grid(True, alpha=0.3)

        progress_cb(100)
        fig.tight_layout()
        return [fig]
```

- [ ] **Step 2: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add Lab3Tab Forward Propagation"
```

---

## Task 6: Lab4Tab — MLP/XOR

**Files:**
- Modify: `week3/week3_app.py`

- [ ] **Step 1: MLP import 헬퍼 추가** (파일 상단 import 블록 아래)

```python
def _import_mlp():
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "p04", os.path.join(os.path.dirname(__file__), "04_mlp_numpy.py")
    )
    mod = importlib.util.module_from_spec(spec)
    import matplotlib.pyplot as _plt
    _orig_show = _plt.show
    _plt.show = lambda *a, **k: None
    spec.loader.exec_module(mod)
    _plt.show = _orig_show
    return mod

_p04 = _import_mlp()
MLP = _p04.MLP
```

- [ ] **Step 2: Lab4Tab 클래스 추가**

```python
class Lab4Tab(BaseLabTab):
    """Lab 4: MLP로 XOR 해결"""

    def _build_param_panel(self, form: QFormLayout):
        self._hidden = QSpinBox()
        self._hidden.setRange(2, 32); self._hidden.setValue(4)
        form.addRow("Hidden Size:", self._hidden)

        self._lr = QDoubleSpinBox()
        self._lr.setRange(0.01, 2.0); self._lr.setSingleStep(0.01); self._lr.setValue(0.5)
        form.addRow("Learning Rate:", self._lr)

        self._epochs = QSpinBox()
        self._epochs.setRange(1000, 50000); self._epochs.setSingleStep(1000)
        self._epochs.setValue(10000)
        form.addRow("Epochs:", self._epochs)

    def _collect_params(self) -> dict:
        return {
            "hidden": self._hidden.value(),
            "lr": self._lr.value(),
            "epochs": self._epochs.value(),
        }

    def _task_fn(self, params, progress_cb, log_cb):
        import numpy as np
        import matplotlib
        matplotlib.use("QtAgg")
        import matplotlib.pyplot as plt

        X = np.array([[0,0],[0,1],[1,0],[1,1]], dtype=float)
        y = np.array([[0],[1],[1],[0]], dtype=float)

        mlp = MLP(
            input_size=2,
            hidden_size=params["hidden"],
            output_size=1,
            learning_rate=params["lr"],
        )

        epochs = params["epochs"]
        report_every = max(1, epochs // 10)

        for epoch in range(epochs):
            output = mlp.forward(X)
            loss = float(np.mean((output - y)**2))
            mlp.backward(X, y, output)
            mlp.loss_history.append(loss)
            if (epoch + 1) % report_every == 0:
                progress_cb(int((epoch+1)/epochs*100))
                log_cb(f"Epoch {epoch+1}/{epochs}  Loss: {loss:.6f}")

        preds = mlp.predict(X)
        acc = float(np.mean(preds == y.astype(int))) * 100
        log_cb(f"\n정확도: {acc:.1f}%")

        fig, axes = plt.subplots(1, 3, figsize=(14, 4))

        # Loss 그래프
        axes[0].plot(mlp.loss_history, lw=2)
        axes[0].set_yscale("log")
        axes[0].set_title("Training Loss (MSE)", fontweight="bold")
        axes[0].set_xlabel("Epoch"); axes[0].set_ylabel("Loss")
        axes[0].grid(True, alpha=0.3)

        # 결정 경계
        xx, yy = np.meshgrid(
            np.linspace(-0.5,1.5,200), np.linspace(-0.5,1.5,200)
        )
        Z = mlp.forward(np.c_[xx.ravel(), yy.ravel()]).reshape(xx.shape)
        cf = axes[1].contourf(xx, yy, Z, levels=20, cmap="RdYlBu", alpha=0.8)
        plt.colorbar(cf, ax=axes[1])
        for point, label in zip(X, y):
            c = "red" if label[0]==1 else "blue"
            m = "o" if label[0]==1 else "x"
            axes[1].scatter(point[0], point[1], c=c, marker=m, s=200,
                            edgecolors="black", lw=2, zorder=5)
        axes[1].set_title("Decision Boundary", fontweight="bold")
        axes[1].grid(True, alpha=0.3)

        # 은닉층 활성화
        ha = mlp.a1
        im = axes[2].imshow(ha.T, cmap="viridis", aspect="auto")
        axes[2].set_yticks(range(params["hidden"]))
        axes[2].set_yticklabels([f"H{i+1}" for i in range(params["hidden"])])
        axes[2].set_xticks(range(4))
        axes[2].set_xticklabels(["(0,0)","(0,1)","(1,0)","(1,1)"])
        axes[2].set_title("Hidden Layer Activations", fontweight="bold")
        plt.colorbar(im, ax=axes[2])
        for i in range(params["hidden"]):
            for j in range(4):
                axes[2].text(j, i, f"{ha[j,i]:.2f}",
                             ha="center", va="center", color="white", fontsize=8)

        fig.tight_layout()
        return [fig]
```

- [ ] **Step 3: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add Lab4Tab MLP XOR"
```

---

## Task 7: Lab5Tab — Universal Approximation

**Files:**
- Modify: `week3/week3_app.py`

- [ ] **Step 1: UniversalApproximator import 헬퍼 추가**

```python
def _import_ua():
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "p05", os.path.join(os.path.dirname(__file__), "05_universal_approximation.py")
    )
    mod = importlib.util.module_from_spec(spec)
    import matplotlib.pyplot as _plt
    _orig_show = _plt.show
    _plt.show = lambda *a, **k: None
    spec.loader.exec_module(mod)
    _plt.show = _orig_show
    return mod

_p05 = _import_ua()
UniversalApproximator = _p05.UniversalApproximator
```

- [ ] **Step 2: Lab5Tab 클래스 추가**

```python
class Lab5Tab(BaseLabTab):
    """Lab 5: Universal Approximation Theorem"""

    def _build_param_panel(self, form: QFormLayout):
        self._fn = QComboBox()
        self._fn.addItems(["Sine", "Step", "Complex"])
        form.addRow("Target Function:", self._fn)

        self._neurons = QComboBox()
        self._neurons.addItems(["3", "10", "50"])
        self._neurons.setCurrentIndex(2)
        form.addRow("Neurons:", self._neurons)

        self._epochs = QSpinBox()
        self._epochs.setRange(1000, 20000); self._epochs.setSingleStep(1000)
        self._epochs.setValue(5000)
        form.addRow("Epochs:", self._epochs)

    def _collect_params(self) -> dict:
        return {
            "fn": self._fn.currentText(),
            "neurons": int(self._neurons.currentText()),
            "epochs": self._epochs.value(),
        }

    def _task_fn(self, params, progress_cb, log_cb):
        import numpy as np
        import matplotlib
        matplotlib.use("QtAgg")
        import matplotlib.pyplot as plt

        fn_map = {
            "Sine":    lambda x: np.sin(2*np.pi*x),
            "Step":    lambda x: np.where(x<0.5, 0, 1).astype(float),
            "Complex": lambda x: np.sin(2*np.pi*x)+0.5*np.sin(4*np.pi*x)+0.3*np.cos(6*np.pi*x),
        }
        target_fn = fn_map[params["fn"]]
        x_train = np.linspace(0,1,100).reshape(-1,1)
        x_test  = np.linspace(0,1,200).reshape(-1,1)
        y_train = target_fn(x_train)
        y_true  = target_fn(x_test)

        n = params["neurons"]
        lr = 0.05 if n < 20 else 0.01
        model = UniversalApproximator(n_hidden=n, activation="tanh")
        epochs = params["epochs"]
        report = max(1, epochs // 10)

        for epoch in range(epochs):
            # inline train step
            z1 = x_train @ model.W1 + model.b1
            a1 = np.tanh(z1)
            out = a1 @ model.W2 + model.b2
            loss = float(np.mean((out-y_train)**2))
            dL = 2*(out-y_train)/len(x_train)
            dW2 = a1.T @ dL; db2 = np.sum(dL, axis=0)
            da1 = dL @ model.W2.T
            dz1 = da1 * (1-a1**2)
            dW1 = x_train.T @ dz1; db1 = np.sum(dz1, axis=0)
            model.W2 -= lr*dW2; model.b2 -= lr*db2
            model.W1 -= lr*dW1; model.b1 -= lr*db1
            if (epoch+1) % report == 0:
                progress_cb(int((epoch+1)/epochs*100))
                log_cb(f"Epoch {epoch+1}/{epochs}  Loss: {loss:.6f}")

        z1 = x_test @ model.W1 + model.b1
        y_pred = (np.tanh(z1) @ model.W2 + model.b2)
        mse = float(np.mean((y_pred - y_true)**2))
        log_cb(f"\nMSE: {mse:.6f}")

        fig, ax = plt.subplots(figsize=(7, 4))
        ax.plot(x_test, y_true, "b-", lw=2, label="True Function", alpha=0.7)
        ax.plot(x_test, y_pred, "r--", lw=2, label=f"NN ({n} neurons)")
        ax.scatter(x_train[::10], y_train[::10], c="green", s=30, alpha=0.5, label="Train data")
        ax.set_title(f"{params['fn']} — {n} neurons  MSE={mse:.4f}", fontweight="bold")
        ax.set_xlabel("x"); ax.set_ylabel("y")
        ax.legend(); ax.grid(True, alpha=0.3)
        fig.tight_layout()
        return [fig]
```

- [ ] **Step 3: MainWindow의 import 구문 수정**

`MainWindow.__init__` 안의 아래 3줄을 제거한다:

```python
# 제거할 코드
from week3_app import Lab1Tab, Lab2Tab, Lab3Tab, Lab4Tab, Lab5Tab
```

직접 참조로 교체 (같은 파일 내 클래스이므로 import 불필요):

```python
self._lab_tabs: list[BaseLabTab] = [
    Lab1Tab(), Lab2Tab(), Lab3Tab(), Lab4Tab(), Lab5Tab()
]
```

- [ ] **Step 4: 전체 앱 실행 확인**

```bash
cd "C:\Users\ansun\OneDrive\바탕 화면\AIandMLcourse\week3"
uv run week3_app.py
```

Expected: 5개 탭 모두 열리고, 각 탭에서 실행 버튼 클릭 시 그래프 표시됨

- [ ] **Step 5: Commit**

```bash
git add week3/week3_app.py
git commit -m "feat: add Lab5Tab Universal Approximation, complete all 5 labs"
```

---

## Task 8: PRD 마크다운 작성

**Files:**
- Create: `week3/prd.md`

- [ ] **Step 1: prd.md 작성**

```markdown
# Product Requirements Document (PRD)
## Week 3 Neural Network 실험 앱

**버전**: 1.0
**날짜**: 2026-04-01
**작성자**: AI/ML 실습 과정

---

## 1. 제품 개요

본 앱은 Week 3에서 학습한 신경망 기초 개념 5가지를 PySide6 GUI를 통해 인터랙티브하게 실험할 수 있는 데스크톱 애플리케이션이다. 학습자는 파라미터를 직접 조절하고 결과를 시각적으로 확인함으로써 이론과 실습을 연결한다.

---

## 2. 목표

- 퍼셉트론, 활성화 함수, 순전파, MLP, 범용 근사 정리를 하나의 앱에서 실험
- 파라미터 변경 → 실행 → 그래프 확인의 단순한 UX로 진입 장벽 최소화
- 기존 Week 3 Python 스크립트 코드 재사용으로 코드 일관성 유지

---

## 3. 사용자 시나리오

1. 학습자가 앱을 실행하면 5개의 탭이 나타난다.
2. 원하는 Lab 탭을 선택하고 파라미터를 설정한다.
3. **실행** 버튼을 누르면 백그라운드에서 연산이 시작된다.
4. 진행 상태바와 로그 창으로 진행 상황을 확인한다.
5. 연산 완료 후 결과 그래프가 오른쪽 패널에 표시된다.
6. 파라미터를 바꿔 다시 실행하며 신경망 동작을 탐색한다.

---

## 4. 기능 명세

### Lab 1: Perceptron
- AND / OR / XOR 게이트를 단일 퍼셉트론으로 학습
- Learning Rate, Epochs 조절 가능
- 출력: 결정 경계 시각화 (contourf + scatter)
- XOR의 선형 분리 불가능성을 시각적으로 확인

### Lab 2: Activation Functions
- Sigmoid, Tanh, ReLU, Leaky ReLU 함수 비교
- X 범위 및 표시할 함수 선택 가능
- 출력: 함수 그래프 + Gradient 그래프

### Lab 3: Forward Propagation
- 2-N-1 네트워크의 순전파 단계별 시각화
- Hidden Units 수, 입력값 X1/X2 조절 가능
- 출력: 네트워크 다이어그램, z/a 값 비교 막대 그래프

### Lab 4: MLP / XOR
- Pure NumPy MLP로 XOR 문제 해결
- Hidden Size, Learning Rate, Epochs 조절 가능
- 출력: Training Loss 곡선, 결정 경계, 은닉층 활성화 히트맵

### Lab 5: Universal Approximation
- Sine / Step / Complex 함수를 단층 신경망으로 근사
- Neurons 수(3/10/50), Epochs 조절 가능
- 출력: 목표 함수 vs 근사 함수 비교 그래프, MSE 표시

---

## 5. UI/UX 요구사항

- 탭 기반 레이아웃: 5개 Lab을 탭으로 구분
- 좌우 분할: 왼쪽 파라미터 패널(260px) / 오른쪽 결과 패널
- 실행 중 모든 탭의 실행 버튼 비활성화 (동시 실행 방지)
- Progress Bar로 진행률 표시 (0~100%)
- 로그 창(read-only)에 에포크별 Loss 출력
- 오류 발생 시 QMessageBox로 안내

---

## 6. 비기능 요구사항

- 실행 중 UI 프리징 없음 (QThread 백그라운드 처리)
- Windows 10/11 환경에서 실행 가능
- Python 3.12+, PySide6 6.6+

---

## 7. 범위 외

- 학습 중 중단(Stop) 기능
- 결과 그래프 저장 기능
- 여러 Lab 동시 실행
```

- [ ] **Step 2: Commit**

```bash
git add week3/prd.md
git commit -m "docs: add PRD for week3 PySide6 app"
```

---

## Task 9: TRD 마크다운 작성

**Files:**
- Create: `week3/trd.md`

- [ ] **Step 1: trd.md 작성**

```markdown
# Technical Requirements Document (TRD)
## Week 3 Neural Network 실험 앱

**버전**: 1.0
**날짜**: 2026-04-01

---

## 1. 기술 스택

| 항목 | 버전 | 용도 |
|------|------|------|
| Python | 3.12+ | 런타임 |
| PySide6 | 6.6+ | GUI 프레임워크 |
| NumPy | 1.26+ | 수치 연산 |
| Matplotlib | 3.8+ | 그래프 생성 |
| reportlab | 최신 | PDF 변환 |

---

## 2. 파일 구조

```
week3/
├── week3_app.py                  # PySide6 앱 진입점
├── 01_perceptron.py              # Perceptron 클래스 (수정 없음)
├── 02_activation_functions.py    # 활성화 함수 (수정 없음)
├── 03_forward_propagation.py     # SimpleNetwork 클래스 (수정 없음)
├── 04_mlp_numpy.py               # MLP 클래스 (수정 없음)
├── 05_universal_approximation.py # UniversalApproximator 클래스 (수정 없음)
├── prd.md / prd.pdf              # PRD 문서
├── trd.md / trd.pdf              # TRD 문서
└── outputs/                      # 기존 출력 폴더
```

---

## 3. 컴포넌트 명세

### TrainingWorker (QThread)

```python
class TrainingWorker(QThread):
    progress    = Signal(int)     # 0~100
    log_message = Signal(str)     # 에포크 로그
    finished    = Signal(object)  # list[matplotlib.Figure]
    error       = Signal(str)     # 예외 메시지
```

- `__init__(task_fn, params)`: 연산 함수와 파라미터 dict 수신
- `run()`: `task_fn(params, progress_cb, log_cb)` 호출, 예외 시 `error` emit

### BaseLabTab (QWidget)

공통 베이스 클래스. 서브클래스가 구현할 추상 메서드:

| 메서드 | 역할 |
|--------|------|
| `_build_param_panel(form)` | 파라미터 위젯을 QFormLayout에 추가 |
| `_collect_params() → dict` | 현재 위젯 값을 dict로 반환 |
| `_task_fn(params, progress_cb, log_cb) → list[Figure]` | 연산 수행, Figure 리스트 반환 |

### 각 LabTab

| 클래스 | import 대상 | 생성 Figure 수 |
|--------|-------------|---------------|
| Lab1Tab | `Perceptron` from 01 | 1 (결정 경계) |
| Lab2Tab | 인라인 함수 | 1 (2-panel) |
| Lab3Tab | 인라인 구현 | 1 (2-panel) |
| Lab4Tab | `MLP` from 04 | 1 (3-panel) |
| Lab5Tab | `UniversalApproximator` from 05 | 1 (비교 그래프) |

---

## 4. 데이터 흐름

```
[사용자] → 파라미터 입력 → 실행 버튼
    → MainWindow._disable_all_runs()
    → TrainingWorker.start()
        → task_fn(params, progress_cb, log_cb)
            → progress Signal → QProgressBar
            → log_message Signal → QTextEdit
            → return [Figure]
        → finished Signal → FigureCanvas 추가
    → MainWindow._enable_all_runs()
```

---

## 5. 기존 코드 import 방식

기존 01~05 파일은 실행 시 `plt.show()` / `plt.savefig()` 를 직접 호출하므로, `importlib.util`로 모듈을 로드하기 전에 `plt.show`를 no-op으로 임시 교체하여 GUI 충돌을 방지한다.

```python
import importlib.util, matplotlib.pyplot as plt

spec = importlib.util.spec_from_file_location("p01", "01_perceptron.py")
mod  = importlib.util.module_from_spec(spec)
plt.show = lambda *a, **k: None   # 임시 억제
spec.loader.exec_module(mod)
plt.show = _orig_show             # 복원
Perceptron = mod.Perceptron
```

---

## 6. 에러 처리

| 상황 | 처리 방법 |
|------|----------|
| 잘못된 파라미터 | `_collect_params()`에서 ValueError raise → QMessageBox.warning |
| 연산 중 예외 | `TrainingWorker.error` signal → QMessageBox.critical |
| 빈 함수 선택 (Lab2) | ValueError("최소 하나의 함수를 선택하세요.") |

---

## 7. 빌드 및 실행

```bash
# 의존성 설치
uv sync

# 앱 실행
cd week3
uv run week3_app.py
```

---

## 8. PDF 변환

PRD/TRD 마크다운을 reportlab으로 PDF 변환:

```bash
cd week3
uv run generate_pdf.py
```

`generate_pdf.py`는 Task 10에서 구현.
```

- [ ] **Step 2: Commit**

```bash
git add week3/trd.md
git commit -m "docs: add TRD for week3 PySide6 app"
```

---

## Task 10: PDF 생성 스크립트

**Files:**
- Create: `week3/generate_pdf.py`

- [ ] **Step 1: generate_pdf.py 작성**

```python
"""PRD/TRD 마크다운 → PDF 변환 (reportlab)"""
import os
from reportlab.lib.pagesizes import A4
from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
from reportlab.lib.units import cm
from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer, Preformatted
from reportlab.lib.enums import TA_LEFT
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont

BASE = os.path.dirname(__file__)
OUTPUT = os.path.join(BASE, "outputs")
os.makedirs(OUTPUT, exist_ok=True)

# 한글 폰트 등록 (Windows Malgun Gothic)
FONT_PATH = r"C:\Windows\Fonts\malgun.ttf"
if os.path.exists(FONT_PATH):
    pdfmetrics.registerFont(TTFont("Malgun", FONT_PATH))
    FONT = "Malgun"
else:
    FONT = "Helvetica"

styles = getSampleStyleSheet()
H1 = ParagraphStyle("h1", fontName=FONT, fontSize=16, spaceAfter=10, spaceBefore=16, leading=22)
H2 = ParagraphStyle("h2", fontName=FONT, fontSize=13, spaceAfter=6,  spaceBefore=12, leading=18)
H3 = ParagraphStyle("h3", fontName=FONT, fontSize=11, spaceAfter=4,  spaceBefore=8,  leading=16)
BODY = ParagraphStyle("body", fontName=FONT, fontSize=9, spaceAfter=4, leading=14)
CODE = ParagraphStyle("code", fontName="Courier", fontSize=8, spaceAfter=4,
                       leftIndent=20, leading=12, backColor="#f5f5f5")


def md_to_flowables(md_text: str) -> list:
    """마크다운을 reportlab Flowable 리스트로 변환 (단순 파서)."""
    flowables = []
    in_code = False
    code_lines = []

    for line in md_text.splitlines():
        if line.startswith("```"):
            if in_code:
                flowables.append(Preformatted("\n".join(code_lines), CODE))
                code_lines = []
                in_code = False
            else:
                in_code = True
            continue

        if in_code:
            code_lines.append(line)
            continue

        if line.startswith("# "):
            flowables.append(Paragraph(line[2:], H1))
        elif line.startswith("## "):
            flowables.append(Paragraph(line[3:], H2))
        elif line.startswith("### "):
            flowables.append(Paragraph(line[4:], H3))
        elif line.startswith("---"):
            flowables.append(Spacer(1, 0.3*cm))
        elif line.strip() == "":
            flowables.append(Spacer(1, 0.2*cm))
        else:
            # 테이블 행은 그냥 텍스트로 처리
            safe = line.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
            flowables.append(Paragraph(safe, BODY))

    return flowables


def convert(src: str, dst: str):
    with open(src, encoding="utf-8") as f:
        md = f.read()
    doc = SimpleDocTemplate(
        dst, pagesize=A4,
        leftMargin=2*cm, rightMargin=2*cm,
        topMargin=2*cm, bottomMargin=2*cm,
    )
    doc.build(md_to_flowables(md))
    print(f"  -> {dst}")


if __name__ == "__main__":
    print("PDF 생성 중...")
    convert(os.path.join(BASE, "prd.md"), os.path.join(OUTPUT, "prd.pdf"))
    convert(os.path.join(BASE, "trd.md"), os.path.join(OUTPUT, "trd.pdf"))
    print("완료!")
```

- [ ] **Step 2: PDF 생성 실행**

```bash
cd "C:\Users\ansun\OneDrive\바탕 화면\AIandMLcourse\week3"
uv run generate_pdf.py
```

Expected:
```
PDF 생성 중...
  -> outputs/prd.pdf
  -> outputs/trd.pdf
완료!
```

- [ ] **Step 3: PDF 파일 존재 확인**

```bash
ls week3/outputs/prd.pdf week3/outputs/trd.pdf
```

Expected: 두 파일 모두 존재

- [ ] **Step 4: Commit**

```bash
git add week3/generate_pdf.py week3/prd.md week3/trd.md week3/outputs/prd.pdf week3/outputs/trd.pdf
git commit -m "feat: add PDF generation script and output PRD/TRD PDFs"
```

---

## Self-Review

**Spec coverage 확인:**

| 요구사항 | Task |
|---------|------|
| 5개 Lab 탭 구현 | Task 3~7 |
| TrainingWorker (QThread) | Task 2 |
| 파라미터 위젯 (모든 Lab) | Task 3~7 |
| Progress Bar + 로그 | Task 2 (BaseLabTab) |
| FigureCanvas 임베드 | Task 2 (BaseLabTab._on_finished) |
| 실행 중 버튼 비활성화 | Task 2 (MainWindow._disable/enable_all_runs) |
| 에러 처리 | Task 2 (BaseLabTab._on_error, _collect_params) |
| PySide6 의존성 | Task 1 |
| PRD PDF | Task 8 + 10 |
| TRD PDF | Task 9 + 10 |

**Placeholder scan:** 없음 — 모든 step에 실제 코드 포함

**Type consistency:** `TrainingWorker.finished` → `object` (list[Figure]), `BaseLabTab._task_fn` → `list` 반환, 모든 탭에서 일치
